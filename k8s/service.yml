apiVersion: v1
kind: Service
metadata:
  name: flask-devops-svc
  namespace: flask-app
spec:
  selector:
    app: flask-devops
  ports:
  - name: http
    port: 80
    targetPort: 5000
  type: ClusterIP
